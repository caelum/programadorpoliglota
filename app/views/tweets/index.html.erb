		<% @tags.each do |tag| %>
			Tag: <%= tag.name %> <br />

			Tweets: 
			<div>
				<ul>
				<% @tweets[tag.name].each do |t| %>					
					<li>
						<a href="http://www.twitter.com/<%=t.user.twitter_id%>">
							<img src='<%= image_url_of(t.user) %>' width='48' height='48'/></a> || 
							<a href="http://www.twitter.com/<%=t.user.twitter_id%>"><%= t.user.twitter_id %></a> 
							-- <%= t.text %><br />
						<small>twittado em <%= t.date.strftime('%d/%m/%Y %H:%M') %></small>
					</li>
				<% end %>
				</ul>
				<%= next_page_link(2, tag)%>
			</div>
			Retuitados:
			<% @retweeted_users[tag.name].each do |r| %>
				<a href='http://www.twitter.com/<%=r.user.twitter_id%>'>
					<img src='<%= image_url_of(r.user) %>' width='48' height='48' />
				</a>
			<% end %>
			Links: 
			<ul>
			<% @links[tag.name].each do |l| %>					
				<li>
					<%= l.quantity %> -- <%= l.title%> <a href="<%= l.url %>"><%= l.url %></a>
				</li>
			<% end%>
			</ul>
			<hr />
		<% end %>
		<script type="text/javascript">
			$(function() {
				registerEvent();
			});
			
			function registerEvent() {
				$(".ver_mais").click(function() {
					elemento = $(this);
					url = $(this).attr('href');
					$.get(url, function(data) {
						elemento.parent().replaceWith(data);
						registerEvent();
					});
					return false;
				});
			}
		</script>